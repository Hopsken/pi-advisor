{
  "name": "pi-advisor",
  "version": "0.1.0",
  "description": "A pi extension that runs an independent advisor agent observing the main agent each turn and injecting graded advice.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "0.80.3"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.80.3",
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
